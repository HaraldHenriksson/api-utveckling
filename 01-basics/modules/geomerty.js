// EVERYTHING GEOMETRY RELATED

const boxArea = (w, h) => {
	return w * h;
}

const boxCircumference = (w, h) => w * 2 + h *2;


module.exports = {
	boxArea, //  && boxArea: boxArea
	boxCircumference,
}
